{#
/**
 * @file
 * Template for a slim  sidebar.
 *
 * This template provides a simple slim sidebar. Meant for node displays.
 *
 * Variables:
 * - id: An optional CSS id to use for the layout.
 * - css_classes: Optional CSS classes to use for the layout.
 * - content: An array of content, each item in the array is keyed to one
 *   panel of the layout. This layout supports the following sections:
 *   - content.top: Content in the bottom top row. Set to be a H2.
 *   - content.slim_sidebar: Content to the left.
 *   - content.main: Main content.
 *   - content.bottom: Content in the bottom row.
 */
#}
<div class="deip-slimsidebarnode">
  <div class="row">
    <div class="medium-12 columns">
      {{ content.top }}
    </div>
  </div>

  <header class="row node-header">
    <div class="medium-9 medium-push-3 small-12 columns">
      {{ content.header }}
    </div>
  </header>

  <div class="row">
    <div class="medium-3 small-12 columns">
      {{ content.slim_sidebar }}
    </div>
    <div class="medium-9 small-12 columns">
      {{ content.main }}
    </div>
  </div>

  <div class="row">
    <div class="medium-12 columns">{{ content.bottom }}</div>
  </div>
</div> <!-- /.deip-slimsidebarnode -->
